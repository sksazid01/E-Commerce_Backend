# Ensure devDependencies are installed during Render builds.
# Render often sets NODE_ENV=production; without this, `npm ci` may omit dev deps
# and break builds (e.g. Nest CLI / TypeScript).
production=false
